@* @model SalesManagement.Models.Invoice


<h2>Invoice #@Model.InvoiceId</h2>
<p>Customer: @Model.Customer.Name</p>
<p>Email: @Model.Customer.Email</p>

<table class="table">
    <tr><th>Product</th><th>Price</th><th>Qty</th><th>Total</th></tr>
    @foreach (var item in Model.Items)
    {
        <tr>
            <td>@item.Product.Name</td>
            <td>@item.Price</td>
            <td>@item.Quantity</td>
            <td>@item.Total</td>
        </tr>
    }
</table>

<h3>Grand Total: @Model.GrandTotal</h3>
<a href="/Invoice/PrintPdf/@Model.InvoiceId" class="btn btn-success" target="_blank">
    Download PDF
</a>
 *@



@model SalesManagement.Models.Invoice

@{
    ViewData["Title"] = $"Invoice #{Model.InvoiceId}";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-9">

            <!-- Header -->
            <div class="card shadow-sm mb-4">
                <div class="card-body d-flex justify-content-between align-items-start">
                    <div>
                        <h4 class="mb-1">Invoice #@Model.InvoiceId</h4>
                        <div class="text-muted">
                            <div><strong>Customer:</strong> @Model.Customer.Name</div>
                            <div><strong>Email:</strong> @Model.Customer.Email</div>
                        </div>
                    </div>

                    <a href="/Invoice/PrintPdf/@Model.InvoiceId"
                       class="btn btn-success"
                       target="_blank">
                        Download PDF
                    </a>
                </div>
            </div>

            <!-- Items -->
            <div class="card shadow-sm">
                <div class="card-body p-0">
                    <table class="table table-striped table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Product</th>
                                <th class="text-end">Unit Price</th>
                                <th class="text-center">Qty</th>
                                <th class="text-end">Total</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Items)
                            {
                                <tr>
                                    <td>@item.Product.Name</td>
                                    <td class="text-end">@item.Price.ToString("C")</td>
                                    <td class="text-center">@item.Quantity</td>
                                    <td class="text-end fw-semibold">
                                        @item.Total.ToString("C")
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot class="table-light">
                            <tr>
                                <th colspan="3" class="text-end">Grand Total</th>
                                <th class="text-end fw-bold">
                                    @Model.GrandTotal.ToString("C")
                                </th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>

            <!-- Footer -->
            <div class="mt-4 text-end">
                <a asp-action="Create" class="btn btn-outline-secondary">
                    Back to List
                </a>
            </div>

        </div>
    </div>
</div>
